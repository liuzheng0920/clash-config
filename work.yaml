parsers: # array
  - url: [此处输入你的节点订阅链接]
    yaml:
      prepend-proxy-groups:  # 在proxy-groups最前面增加代理分组
        - name: hngtrust
          type: select
          proxies:
            - 🔰 选择节点
            - DIRECT
      prepend-rules:  # 在rules最前面增加规则
        - DOMAIN-SUFFIX,hngtrust.com,ChatGPT
